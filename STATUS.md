- 2026-09-01 (mac-agent): WMS evidence images **deliberately NOT refreshed** (Rick's call). Two independent locks confirmed by inspection, not assumption: (1) saving a programmatically-set file item fails every time with *"Session state protection violation ... P320_UPLOAD_SCREENSHOT"* — retested on a clean page load to rule out a stale checksum from the tab re-render, same error, so the trigger is the **status**, not page freshness (the identical technique succeeded on 8/31 while the workshop was at *Self QA*); (2) **all nine status buttons are `pointer-events:none; cursor:not-allowed`**, so the owner cannot move Self QA Complete back to Self QA to re-open the checklist. Note the DOM reports these items `disabled=false`/`readOnly=false` — editable client-side, rejected server-side; trust the server. Nothing was altered: verified after each failed save that status, 31/31 boxes and the PR link were intact. Net position: WMS checklist **answers** remain accurate; only the three evidence images predate the provisioning lab (the lint image quotes 38 PNGs vs 52 today). **PR #346 is the source of truth for reviewers**, and the current images live in `docs/self-qa/` ready for re-upload when the checklist re-opens at Quarterly QA.
